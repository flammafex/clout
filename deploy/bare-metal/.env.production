# ============================================
# Clout Production Configuration
# For: cloutsocial.net on Hetzner CX11
# ============================================

# ----------------------------------------
# Server Configuration
# ----------------------------------------
NODE_ENV=production
PORT=3000

# ----------------------------------------
# Instance Information (displayed to visitors)
# ----------------------------------------
INSTANCE_NAME=cloutsocial.net
INSTANCE_OPERATOR=Your Name Here
INSTANCE_DESCRIPTION=A community Clout instance

# Instance owner's browser public key (for admin access)
# Get this from Profile tab after creating your browser identity
INSTANCE_OWNER_PUBKEY=

# Require authentication for API endpoints
CLOUT_AUTH=false

# Private mode - set to true to require authentication for viewing
CLOUT_PRIVATE=false

# ----------------------------------------
# Data Storage
# ----------------------------------------
CLOUT_DATA_DIR=/var/lib/clout

# ----------------------------------------
# External Services - Your Infrastructure
# ----------------------------------------

# Witness Gateway (your witness cluster)
# Using witness1 as primary - Clout will try all gateways
WITNESS_GATEWAY_URL=https://witness1.metacan.org
WITNESS_GATEWAY_URL_2=https://witness2.metacan.org
WITNESS_GATEWAY_URL_3=https://witness3.metacan.org
WITNESS_NETWORK_ID=clout-mainnet

# Freebird - Anonymous Authorization
FREEBIRD_ISSUER_URL=https://issuer.metacan.org
FREEBIRD_VERIFIER_URL=https://verifier.metacan.org

# Sybil resistance mode (invitation = web-of-trust)
FREEBIRD_SYBIL_MODE=invitation

# Admin key for owner registration (CHANGE THIS!)
# Generate with: openssl rand -hex 32
FREEBIRD_ADMIN_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_32

# ----------------------------------------
# P2P Networking
# ----------------------------------------
# HyperToken relay - using self (Clout includes relay functionality)
HYPERTOKEN_RELAY_URL=wss://cloutsocial.net

# ----------------------------------------
# Tor Configuration (Disabled)
# ----------------------------------------
TOR_ENABLED=false

# ----------------------------------------
# Debug (Disabled for production)
# ----------------------------------------
DEBUG=false
